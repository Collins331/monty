#include "monty.h"
/**
 * main - entry point
 * @argc: the number of argument
 * @argv: the arguments
 * Return: 0(success) else error set appropriately
 */
int main(int argc, char **argv)
{
	(void)argv;
	arguments(argc);
	return (0);
}
